Command:
python3 host_checks.py

Output (2026-08-28):
PASS: storage packing round-trips through 10,000 randomized frames
PASS: 20 h backlog + 15% margin is under 350 MB
PASS: SD, D7 button, D6 haptic, Audio BFF, and reserved IMU I2C pins do not conflict
PASS: critical source fixes are present
HARDWARE GATE: measured 16 h average must be <= 26.56 mA

Patch verification:
`anticipy_evt_firmware.patch` applied cleanly with `patch -p1` to a fresh copy
of Omi's DevKit source; all six resulting source/overlay files matched this pack.

Build-metadata verification:
`west.yml` and `.github/workflows/build-evt-firmware.yml` parsed as YAML.

Scope:
This is a host-side model/static check. It is not a Zephyr compile, BLE test,
SD-card test, battery measurement, or physical hardware test.
